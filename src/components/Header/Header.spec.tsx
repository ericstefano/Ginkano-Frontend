describe('header', () => {
  it('should render', () => {
    //    render(<Header> Teste</Header>);
    //    const header = screen.getByText(/Teste/i);
    //    expect(header).toBeInTheDocument();

    expect(1).toBe(1);
  });
});
